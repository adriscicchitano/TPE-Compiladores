double aa;
uint func f(uint z)
uint a,b;
begin
	pre: (x>0), %+%;
	a:=a - 4;
	a:= a + 1;
	return (0);
end;
func r,t;
begin
	x:= f (uint j) * -3.1E3;
	while (x && 0) DO
	begin
		x:=3;
		break;
	end;
	print(%aa%);
	if (x>0)  then begin a:=0; end else begin a:=0; end endif;
end
$